# Job 35154 review

- Slurm/accounting: `COMPLETED`, exit `0:0`, elapsed `00:12:08` on `tko-b3-nv-dgx06`; `run.exitcode` is `0` and the required report exists.
- The same pending job id was retargeted from dgx04 after that node became full to best-fit fragment node dgx06. No duplicate `sbatch` occurred.
- The prepare-time required-output path accidentally included an extra `outputs/` component. The manifest path metadata was corrected after completion; the generated result was not modified.
- Full 340M model, sequence 4096, micro batch 1, activation checkpointing, BF16 mixed precision; 5 warmup and 50 measured steps per isolated process.
- QGDN chunk 32: `15840.4` token/s, median `254.82 ms`, peak `6.844` GB.
- QGDN chunk 32 + compiled input builder: `16140.2` token/s, median `247.70 ms`, peak `6.794` GB.
- Compiled input builder improves mean throughput by `1.89%`, median step time by `2.88%`, and lowers peak memory by `0.050` GB. Its throughput is `90.87%` of the isolated GDN control (`17761.3` token/s).
- Builder outputs match exactly; gradients are finite with maximum relative RMSE `3.7721e-06`. Final full-model loss differs from eager chunk 32 by `5.46e-08`.
- Decision: enable compiled input construction in production. Keep the exact QGDN recurrence; separately benchmark disabling DPLR backward recomputation for a possible additional speed/memory tradeoff.
