# Job 35171 review

- Slurm/accounting: `COMPLETED`, exit `0:0`, elapsed `00:01:33` on `tko-b3-nv-dgx16`; required JSON and `run.exitcode=0` are present.
- Focused isolated 340M/4096/BF16 pair, 5 warmup and 50 measured steps per variant.
- Compiled inputs + normal recompute: `12329.5` token/s, mean `332.21 ms`, median `335.44 ms`, peak `6.786` GB.
- Compiled inputs + no recompute: `13521.0` token/s, mean `302.94 ms`, median `312.17 ms`, peak `6.782` GB.
- No-recompute improves mean throughput by `9.66%` and median step time by `7.45%`; final loss is bit-identical and peak memory changes by `-0.0037` GB.
- Decision: candidate is positive, but both completed pairs used the same normal-then-no-recompute order. Run one reverse-order pair before enabling the default.
