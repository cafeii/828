# Job 35125 review

- Slurm/accounting: `COMPLETED`, exit `0:0`, elapsed `00:05:10` on `tko-b3-nv-dgx04`; `run.exitcode` is `0` and the required report exists.
- Full 340M model, sequence 4096, micro batch 1, activation checkpointing, BF16 mixed precision; every variant ran in a fresh Python/CUDA process.
- Compiled builder vs eager: all six outputs match exactly; all gradients are finite and maximum relative RMSE is `3.7721e-06`.
- QGDN chunk 16: `10588.1` token/s, `6.853` GB peak.
- QGDN chunk 32: `11435.6` token/s, `6.835` GB peak.
- QGDN chunk 32 + compiled input builder: `11669.2` token/s, `6.794` GB peak.
- The compiled builder improves mean throughput by `2.04%` over chunk 32 and `10.21%` over chunk 16. Median step time improves from `356.47 ms` to `322.22 ms`, but one `604.34 ms` outlier makes the 10-step mean noisy.
- Decision: candidate is numerically valid but not enabled yet. Run a longer isolated confirmation before changing production; the 90% GDN target remains unmet.
