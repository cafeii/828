# Job 35173 review

- Slurm/accounting: `COMPLETED`, exit `0:0`, elapsed `00:01:34` on `tko-b3-nv-dgx16`; required JSON and `run.exitcode=0` are present.
- Reverse-order isolated 340M/4096/BF16 pair, 5 warmup and 50 measured steps per variant.
- No-recompute first: `11683.7` token/s, mean `350.58 ms`, median `348.03 ms`, peak `6.782` GB.
- Normal recompute second: `14446.2` token/s, mean `283.53 ms`, median `255.47 ms`, peak `6.786` GB.
- In reverse order, no-recompute is `19.12%` slower. Final loss is bit-identical and memory difference remains negligible.
- The forward and reverse pair conclusions have opposite signs. Decision: reject no-recompute as an unstable/non-reproducible speed change and retain `QGDN_DISABLE_DPLR_RECOMPUTE=False`.
