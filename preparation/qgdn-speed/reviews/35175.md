# Job 35175 review

- Slurm/accounting: `COMPLETED`, exit `0:0`, elapsed `00:05:12` on the full node `tko-b3-nv-dgx39`; `run.exitcode` is `0`.
- Required `qgdn-ddp/summary.json` exists. `run.json`, `summary.json`, and `metrics.jsonl` identities agree on commit `7628d592ed1d4eafbba6bd81ea7bb9940a37fe88`.
- Configuration: QGDN 340M, 8 GPUs, BF16 mixed precision, sequence length 4096, global batch 16, micro batch 1, 2 training steps, activation checkpointing enabled.
- All 8 ranks completed forward, backward, optimizer update, checkpointing, and validation. Loss, gradient norm, and gate statistics are finite.
- The first training step includes one-time compilation (`102.34 s`). The second steady step took `0.6802 s`, or `96,350.2 token/s` globally. Peak memory was `9.383 GB` per reported rank.
- The smoke validation used synthetic MQAR data and only 8 sequences, so its loss is a functional check rather than a language-model quality result.
- No substantive error was found. The logged `pkg_resources`, TF32 API, and NCCL device-selection messages are warnings only.
- Decision: the compiled QGDN input construction is accepted for 8-GPU DDP. The production recurrence and numerics remain unchanged; no FineWeb pretraining was submitted.
