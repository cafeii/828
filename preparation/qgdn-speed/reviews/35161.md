# Job 35161 review

- Slurm/accounting: `COMPLETED`, exit `0:0`, elapsed `00:11:58` on `tko-b3-nv-dgx16`; required JSON and `run.exitcode=0` are present.
- Isolated 340M/4096/BF16 training benchmark, 2 warmup and 10 measured steps per variant.
- Compiled input + normal DPLR recompute: `11008.7` token/s, median `326.35 ms`, peak `6.794` GB.
- Compiled input + no DPLR recompute: `16551.5` token/s, median `247.39 ms`, peak `6.782` GB; final loss is bit-identical to normal recompute.
- The no-recompute timings are stable, but the normal-recompute control was much slower than its separate 50-step result (`247.70 ms` median). Therefore the apparent `1.50x` speedup is not accepted as a stable effect yet.
- Decision: run a longer same-job pair with 5 warmup and 50 measured steps. Enable no-recompute only if it is non-regressive and memory remains reasonable.
