# Job 35120 review

- Slurm/accounting: `COMPLETED`, exit `0:0`, elapsed `00:00:43` on `tko-b3-nv-dgx04`.
- Required report exists; `run.exitcode` is `0`.
- Reverse order was QGDN chunk 32, QGDN chunk 16, then GDN.
- Chunk 32 first: 11158.9 token/s. Chunk 16 second: 7835.8 token/s.
- In job 35101 the second QGDN variant was chunk 32 and it was slower; here the second variant was chunk 16 and it was slower. The rank reversal follows measurement order, not chunk size.
- Decision: neither multi-model result selects a production chunk size. Benchmark each variant in a fresh process before accepting or reverting the candidate.
