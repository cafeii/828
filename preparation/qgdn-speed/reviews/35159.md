# Job 35159 review

- Slurm/accounting: `COMPLETED`, exit `0:0`, elapsed `00:01:31` on best-fit fragment node `tko-b3-nv-dgx16`.
- `run.exitcode` is `0`; required JUnit report exists and was collected.
- CUDA compiled/eager virtual-input parity: `2 passed`, `27 deselected` in `75.92s`, covering query and key recall outputs plus all input gradients.
- Production commit `da5320e124122dbd92abcbc08b81d0fc378f6be8` passes 15 non-CUDA QGDN tests and this CUDA parity gate.
- Decision: compiled virtual-input construction is accepted. Continue only with the independent DPLR no-recompute performance candidate.
