# Job 35096 review

- Slurm/accounting: `FAILED`, exit `1:0`, elapsed `00:09:45` on `tko-b3-nv-dgx04`.
- `run.exitcode`: `1`; required `chunk-size-profile.json` is absent.
- First substantive error: the final GDN baseline benchmark differentiated all seven QGDN inputs, although native GDN intentionally does not use `gamma`. PyTorch rejected the unused tensor.
- QGDN chunk candidates had already run, but the script writes the report only after GDN, so no partial metrics are accepted.
- Fix: use `allow_unused=True` only in the generic timing helper. This changes benchmark bookkeeping, not any model or kernel. One same-cause retry is permitted.
