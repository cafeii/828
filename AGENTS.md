# AGENTS.md

本文件为Agent工作时提供指引。

## 项目简介

这是一个探索RNN前沿架构的项目，目标为在现有工作（GDN、KDA、GDN2）的基础上对结构进行改进。当前探索内容为LSR (Latent State RNN)，详细内容在docs/research.md中。

## 环境介绍

### 本地环境

路径：`/Users/chen/workspace/rnn`

工作区结构
- docs: 研究文档
- plans: Codex的工作计划
- third_party: 外部资源库
- dataset: 存放数据集（如有）
- model: 模型代码文件夹
- scripts: 存放实验代码的文件夹
- tests: 开发用的测试脚本的文件夹
- tools: 处理一些临时需求的小工具
- logs: 存放各种logs
- outputs: 存放各种输出结果（ckpts、evals等）

管理标准：所有文件、代码，分门别类放好，必要时可以创建子文件夹
开发规则：需要复用第三方代码时，在对应文件夹下创建代码或者文件夹；需要对外部代码、导入的第三方包进行改动时，以补丁注入的方式进行，在目标文件夹下创建patches/文件夹，其中PATCHES.md文件记录每一处改动
环境隔离：本工作区内使用uv进行环境管理
网络环境：本地开启clash代理tun模式，流量默认走1082端口。访问中国大陆网址前需设置no proxy

### 服务器环境

连接方式：`ssh slurmer@10.144.2.131`
工作区路径：`/work/projects/memos-b3/code/lzc/rnn`
数据集路径：`/work/projects/memos-b3/datasets`
环境隔离：服务器账号为多人共用，使用conda进行环境隔离，`lzc-*`的conda环境为可用环境。禁止擅自安装、更改服务器任何内容，禁止修改非工作区以外的任何内容，如必须修改需向用户申请。服务器位于中国大陆，无法直接访问境外地址
网络环境：服务器位于中国大陆，无法直接访问境外地址，可访问github，详见`run-remote-experiment` skill

## Rules（必须遵守）

- 使用中文进行思考和回复
- 对工作区外的修改提交用户审核
- 任务若可并行，应进行拆解并分配给subagents
- 向用户提问：开发前先思考，保持与用户的认知对齐，向用户说清楚你的假设，列出权衡利弊，如果有更好的方案，明确指出；拒绝直接做假设，避免任何细节未对齐就直接开发
- 遇到自己解决不了的问题时，向用户求助
- 代码：风格上要求简洁易读，逻辑上追求最优实现，功能上拒绝过度开发